#include <iostream>

using namespace std; 

int main() {
	
	double a1,a2,a,b;
	cin >> a >> b;
	a = abs(a);
	b = abs(b);
	cout << "a+b " << a+b << endl;
	cout << "a-b " << a - b << endl;
	cout << "a*b " << a * b<< endl;
	cout << "a/b " << a / b << endl;

	system("PAUSE");
	return 0;
}
